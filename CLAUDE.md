# 案件8: AI搭載 売上分析ダッシュボード（アパレルEC「LUMINA」）

AIエンジニア講座の最終案件。
Step 1（ヒアリング）と Step 2（設計・提案）は講師提出済み・完了。
**このディレクトリは Step 3（実装）用。**

## 確定仕様

- 納期 14営業日 / 初期費用 44万円（税込）/ 月額 約3,900円
- スタック: Next.js (Vercel Pro) + Supabase (Free) + Claude API + Recharts
- CI/CD: GitHub Actions（型チェック・Lint・AI分析のSnapshotテスト）
- ブランドカラー: 白 + ネイビー `#1A2E5C`
- 読み手7名（社長・マーケ部長・営業5名）、期間軸は月次のみ（日次はPhase 2）
- 絶対KPI: 売上 / 粗利 / リピート率
- AI分析: サマリー + アクション提案（コンサルトーン）、JSON構造化で型を固定

MVPスコープ・Phase 2・検収基準の正本は `docs/proposal-final.md`。
**実装のゴールは提案書に書いた約束を満たすこと。迷ったら提案書を読む。**

実装時に決めた判断（リピート率の期間軸・SKUランキングの基準・講義コードから変えた点）は
`docs/decisions.md` に記録している。理由なしに講義版へ戻さないこと。

## 講義版から意図的に変えた点（戻さないこと）

| 項目 | 講義版 | 本案件 | 理由 |
|---|---|---|---|
| 納期 | 10営業日 | **14営業日** | 工程ごとに逆算した結果 |
| Vercel | 無料(Hobby) | **Pro $20/月** | Hobbyは非商用限定。受託では規約違反 |
| Supabase停止対策 | なし | **Vercel Cronで日次アクセス** | Freeは1週間の無操作で自動停止する |
| 障害検知 | なし | **UptimeRobot（外部・無料）** | 監視は監視対象の外に置く原則 |
| Claude API費用 | 月1,000〜2,000円 | **月500円で計上** | 月1回×50円なら50円。講義の数字は算数が合わない |

**Supabaseの停止対策とバックアップは Vercel Cron で行う。**
GitHub Actions のスケジュール実行は使わない（リポジトリが60日無活動だと自動停止するため、
保守契約が切れた後に静かに壊れる）。GitHub Actions は CI/CD（push / PR）専用。

## 着手手順（1日目）

1. **サンプルCSVのREADMEを読み、リピート率の定義を確定する**
   - 講義がREADMEに正解値（月次売上・粗利・カテゴリ別・リピート率）を用意している
   - 定義は自分で決めず、正解値から逆算して合わせる
   - 案件7 Step4のFB: **正解値そのものを原文と突き合わせて疑う。** 食い違いは実装前に潰す
2. サンプルは40件・2025年9〜11月。カラムは
   `order_date / customer_id / product_name / category / sku / quantity / revenue / cost`
   - 11月に冬物アウターが急増する。AI分析が「アウターが牽引」と読めるかが見どころ
3. Supabase → Next.js → Vercel の順で構築

## 通知経路の実測（デプロイ後にやる）

`/api/health`（Supabaseへ軽いクエリ → 200 / 失敗時500）を作ってデプロイしてから:

1. UptimeRobot登録（無料・カード不要）、5分間隔のHTTP(s)監視を設定
2. 監視URLを存在しないパスに変え、**メールが届くまでの実時間を測る**
3. 提案書には「10分程度が目安」と書いた。**実測が大きくずれたら提案書側を直す**
   （UptimeRobotは1回のチェック＋複数地点から2〜3回の再確認をしてから通知する）
4. `HEALTH_FORCE_FAIL` 環境変数で500を返す分岐も仕込む（検知ロジックの検証用）

## 使うスキル

- 提案書・検収基準まわり: `teiansho-sakusei`（案件1〜8の講師FBを統合した正本）
- 課題完了後の記事: `kiji-sakusei`
- 外に出す前の品質チェック: `ガン`（提出・送信の前に必ず通す）

## 注意: next dev による CLAUDE.md の上書き

`next dev` は起動のたびに `AGENTS.md` を生成し、`CLAUDE.md` を `@AGENTS.md` の1行で
**上書きする**（2026-08-19に実際に発生し、上記の内容が全消失した）。
本ファイルは末尾で `AGENTS.md` を読み込む形にして共存させている。
`CLAUDE.md` が1行になっていたら上書きされているので、git から復元すること。

@AGENTS.md
